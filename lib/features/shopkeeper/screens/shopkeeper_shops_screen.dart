import 'package:flutter/material.dart';

class ShopkeeperShopsScreen extends StatelessWidget {
  const ShopkeeperShopsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Shop shops"),
      ),
    );
  }
}